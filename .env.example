# Prefer using environment variables for Google credentials. Do NOT commit client_secret.json.
# Example environment variables for local development and deployment.

# Flask
SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# Google Calendar (required in production)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Redirect URI should match the one configured in Google Cloud Console
GOOGLE_REDIRECT_URI=http://localhost:5000/calendar/oauth2callback

# Database
DATABASE_URL=sqlite:///database/app.db

# Token encryption key (base64 urlsafe, 32 bytes). Example: generate with Python
# >>> import base64, os; base64.urlsafe_b64encode(os.urandom(32)).decode()
TOKEN_ENCRYPTION_KEY=

# Redis for server-side sessions (recommended for production)
REDIS_URL=redis://localhost:6379/0

# CORS origins (comma separated)
CORS_ORIGINS=http://localhost:5173,https://www.equilibriocognitivo.com.br

# Optional email settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Optional ERP integration
ERP_BASE_URL=https://your-erp-api.com
ERP_TOKEN=your-erp-token

# Cookie flags (set to 1 in production with HTTPS)
SESSION_COOKIE_SECURE=1
SESSION_COOKIE_HTTPONLY=1
SESSION_COOKIE_SAMESITE=Lax

# Notes:
# - Never commit real secrets (client secrets, tokens, keys) to version control.
# - Rotate Google OAuth client secret immediately if it was ever committed.# NOTE: Prefer using environment variables for Google credentials. Do NOT commit client_secret.json.
# Configurações do Flask
```dotenv
# Configurações do Flask
SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# Configurações do Google Calendar
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5000/calendar/callback

# Configurações do Banco de Dados
DATABASE_URL=sqlite:///database/app.db

# Chave para criptografia de tokens (base64 urlsafe). Ex.: gerada com os.urandom(32) + base64.urlsafe_b64encode
TOKEN_ENCRYPTION_KEY=

# Redis para sessões (recomendado em produção)
REDIS_URL=redis://localhost:6379/0

# Configurações de CORS
CORS_ORIGINS=http://localhost:5173,https://www.equilibriocognitivo.com.br

# Configurações de Email (opcional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Configurações de ERP (opcional)
ERP_BASE_URL=https://your-erp-api.com
ERP_TOKEN=your-erp-token

# Flags de cookie para produção
SESSION_COOKIE_SECURE=1  # habilitar em https
SESSION_COOKIE_HTTPONLY=1
SESSION_COOKIE_SAMESITE=Lax

